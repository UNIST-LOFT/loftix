diff --git a/fuzzolic/binradar.py b/fuzzolic/binradar.py
index 93593a664ff0..f6cbba3f76c7 100644
--- a/fuzzolic/binradar.py
+++ b/fuzzolic/binradar.py
@@ -22,11 +22,11 @@ import fcntl
 from types import TracebackType
 from typing import Dict, List, Tuple, Set, Optional, TextIO, BinaryIO
 
-import binradar_verifier
-import binradar_fuzzer
-import binradar_minimizer
-import binradar_utils
-import logger
+from . import binradar_verifier
+from . import binradar_fuzzer
+from . import binradar_minimizer
+from . import binradar_utils
+from . import logger
 import sbsv
 
 SCRIPT_DIR = os.path.dirname(os.path.realpath(__file__))
diff --git a/fuzzolic/binradar_fuzzer.py b/fuzzolic/binradar_fuzzer.py
index 5592d83e6035..b5d73769a96b 100644
--- a/fuzzolic/binradar_fuzzer.py
+++ b/fuzzolic/binradar_fuzzer.py
@@ -8,9 +8,8 @@ from typing import List, Set, Tuple, Dict, Optional, Any, TextIO
 
 import sbsv
 
-import logger
-
-import binradar_utils
+from . import binradar_utils
+from . import logger
 
 ROOT_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
 QEMU_TARGETED_SIMPLE_RELEASE = os.path.join(ROOT_DIR, "LibAFL", "fuzzers", "binary_only", "qemu_targeted_simple", "target", "release", "qemu_targeted_simple")
diff --git a/fuzzolic/binradar_minimizer.py b/fuzzolic/binradar_minimizer.py
index 931c06c539d9..49d16bc4189e 100644
--- a/fuzzolic/binradar_minimizer.py
+++ b/fuzzolic/binradar_minimizer.py
@@ -12,7 +12,7 @@ import time
 
 from typing import List, Dict, Set, Tuple, Any, Optional
 
-import binradar_verifier
+from . import binradar_verifier
 
 ROOT_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
 QEMU_STACKTRACE_RELEASE = os.path.join(ROOT_DIR, "LibAFL", "fuzzers", "binary_only", "qemu_stacktrace", "target", "release", "qemu_stacktrace")
diff --git a/fuzzolic/binradar-setup.py b/fuzzolic/binradar_setup.py
similarity index 99%
rename from fuzzolic/binradar-setup.py
rename to fuzzolic/binradar_setup.py
index 358f9c0d010e..212b2998fdbc 100755
--- a/fuzzolic/binradar-setup.py
+++ b/fuzzolic/binradar_setup.py
@@ -14,6 +14,7 @@ import tempfile
 import threading
 import time
 from dataclasses import dataclass
+from importlib import resources
 from pathlib import Path
 from typing import Dict, List, Optional, Tuple, Union
 
@@ -21,9 +21,9 @@ SCRIPT_DIR = Path(__file__).parent.resolve()
 if str(SCRIPT_DIR) not in sys.path:
     sys.path.insert(0, str(SCRIPT_DIR))
 
-import binradar_taosc_predicates
-import binradar_utils
-from binradar_taosc_predicates import (
+from . import binradar_taosc_predicates
+from . import binradar_utils
+from .binradar_taosc_predicates import (
     AllocatorTrace,
     CWE805PointerPredicate,
     CWE805SizePredicate,
@@ -61,11 +62,13 @@ from binradar_taosc_predicates import (
     write_runtime_predicates,
 )
 
-ROOT_DIR = SCRIPT_DIR.parent
-BENCHMARK_SCRIPTS = ROOT_DIR / "benchmarks" / "scripts"
-BRPATCH_SOURCE = ROOT_DIR / "benchmarks" / "loftix" / "brpatch.c"
-BRPATCH_PREFILTER_SOURCE = ROOT_DIR / "benchmarks" / "loftix" / "brpatch-prefilter.c"
-BRPATCH_CACHED_SOURCE = ROOT_DIR / "benchmarks" / "loftix" / "brpatch-cached.c"
+BENCHMARK_GET_BINARY = resources.path("fuzzolic.benchmarks.scripts",
+                                      "binradar_get_binary.py")
+BRPATCH_SOURCE = resources.path("fuzzolic.benchmarks.loftix", "brpatch.c")
+BRPATCH_PREFILTER_SOURCE = resources.path("fuzzolic.benchmarks.loftix",
+                                          "brpatch-prefilter.c")
+BRPATCH_CACHED_SOURCE = resources.path("fuzzolic.benchmarks.loftix",
+                                       "brpatch-cached.c")
 QEMU_STACKTRACE_RELEASE = ROOT_DIR / "utils" / "binradar-aflplusplus" / "afl-qemu-trace"
 
 
@@ -157,8 +159,8 @@ def ensure_original_binary(workdir: Path, configdir: Path, config: dict) -> Path
     original_binary = workdir / f"{binary}.orig"
     if original_binary.exists():
         return original_binary
-    cmd = [sys.executable, str(BENCHMARK_SCRIPTS / "binradar_get_binary.py"),
-           "-c", str(configdir)]
+    with BENCHMARK_GET_BINARY as resource:
+        cmd = [sys.executable, str(resource), "-c", str(configdir)]
     result = subprocess.run(cmd, capture_output=True, text=True)
     if result.returncode != 0:
         raise RuntimeError(
@@ -191,7 +194,8 @@ def compile_capture_plugin(workdir: Path,
     capture in (BRPATCH_CWE805 + the allocator kind define); generic
     families keep the sbsv register capture.
     """
-    shutil.copy(BRPATCH_PREFILTER_SOURCE, workdir / "brpatch-prefilter.c")
+    with BRPATCH_PREFILTER_SOURCE as resource:
+        shutil.copy(resource, workdir / "brpatch-prefilter.c")
     cmd = ["guix", "shell", "e9patch@1.0.1", "--",
            "e9compile", "brpatch-prefilter.c", "-DTAOSC_DEST=0"]
     if allocator is not None:
@@ -264,7 +268,8 @@ def build_cached_binary(
         raise RuntimeError("brpatch.c and brpatches.inc must be prepared "
                            "before building .brcached")
 
-    shutil.copy(BRPATCH_CACHED_SOURCE, workdir / "brpatch-cached.c")
+    with BRPATCH_CACHED_SOURCE as resource:
+        shutil.copy(resource, workdir / "brpatch-cached.c")
     destinations_file = workdir / "patch-destination"
     if not destinations_file.exists():
         destinations_file_original = workdir / "destinations"
@@ -1055,7 +1060,8 @@ def prepare_patch(configdir: Path, workdir: Path, binradar_env: Dict[str, str]):
             print(f"Error: no destination found in {destinations_file}")
             exit(1)
         brpatch_source = workdir / "brpatch.c"
-        shutil.copy(BRPATCH_SOURCE, brpatch_source)
+        with BRPATCH_SOURCE as resource:
+            shutil.copy(resource, brpatch_source)
         brpatches_inc = workdir / "brpatches.inc"
         _emit_brpatches_inc(brpatches_inc, [])
         compile_defines = [f"-DTAOSC_DEST={dest}", "-DBRPATCH_CWE805",
@@ -1235,7 +1241,8 @@ def prepare_patch(configdir: Path, workdir: Path, binradar_env: Dict[str, str]):
     patch_cnt = len(selected_patch_records)
     binradar_env["TOTAL_PATCHES"] = str(patch_cnt)
     brpatch_source = workdir / "brpatch.c"
-    shutil.copy(BRPATCH_SOURCE, brpatch_source)
+    with BRPATCH_SOURCE as resource:
+        shutil.copy(resource, brpatch_source)
     brpatches_inc = workdir / "brpatches.inc"
     _emit_brpatches_inc(brpatches_inc, selected_patch_records)
     compile_defines = [f"-DTAOSC_DEST={dest}"]
diff --git a/fuzzolic/binradar_utils.py b/fuzzolic/binradar_utils.py
index 42f8aa400d0f..58fc56173af9 100644
--- a/fuzzolic/binradar_utils.py
+++ b/fuzzolic/binradar_utils.py
@@ -4,7 +4,7 @@ import signal
 import threading
 from typing import List, Set, Tuple, Dict, Optional, Any
 
-import logger
+from . import logger
 
 class ExecutionResult:
     def __init__(self, success: bool, exit_code: int, stdout: str, stderr: str):
diff --git a/fuzzolic/binradar_verifier.py b/fuzzolic/binradar_verifier.py
index be26f4b40173..1724a06b311a 100644
--- a/fuzzolic/binradar_verifier.py
+++ b/fuzzolic/binradar_verifier.py
@@ -11,10 +11,9 @@ from typing import List, Set, Tuple, Dict, Optional, Any, TextIO
 
 import sbsv
 
-import logger
-
-import binradar_utils
-from binradar_taosc_predicates import (
+from . import binradar_utils
+from . import logger
+from .binradar_taosc_predicates import (
     CachedSnapshot,
     ParsedPredicate,
     PredicateFamily,
diff --git a/pyproject.toml b/pyproject.toml
index fb546dcb453e..f04387bf7f53 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -1,3 +1,7 @@
+[build-system]
+requires = ['flit_core >=3.2']
+build-backend = 'flit_core.buildapi'
+
 [project]
 name = "fuzzolic"
 version = "0.1.0"
@@ -5,7 +9,15 @@ description = "Add your description here"
 readme = "README.md"
 requires-python = ">=3.10"
 dependencies = [
     "pyelftools>=0.31",
-    "pytest>=8.3.5",
     "sbsv>=0.2.3,<0.3.0",
 ]
+
+[project.optional-dependencies]
+test = [
+    "pytest>=8.3.5",
+]
+
+[project.scripts]
+binradar = 'fuzzolic.binradar:main'
+binradar-setup = 'fuzzolic.binradar_setup:main'
